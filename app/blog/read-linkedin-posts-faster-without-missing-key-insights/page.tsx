import Link from 'next/link';
import { Metadata } from 'next';
import BlogContent from './BlogContent';

export const metadata: Metadata = {
  title: 'How to Read LinkedIn Posts Faster Without Missing Key Insights | Requill',
  description: 'Use Requill to instantly summarize LinkedIn posts while preserving key context and critical details.',
};

export default function ReadLinkedInPostsFasterPost() {
  return <BlogContent />;
}